<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>phillyy | portfolio</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <!-- Importing EB Garamond and a few others for the dynamic effect -->
    <link href="https://fonts.googleapis.com/css2?family=EB+Garamond:ital,wght@0,400;0,500;1,400&family=Inter:wght@300;400&family=Playfair+Display:ital@0;1&family=Courier+Prime&family=Libre+Baskerville:ital@0;1&display=swap" rel="stylesheet">
    <style>
        :root {
            --bg-color: #ffffff;
            --text-main: #333333; /* Lighter shade of black */
            --text-accent: #777777;
            --adobe-red: #fa0f00;
            --phillis-blue: #afeeee; /* Turquoise blue pastel */
            --border-color: #eeeeee;
            --nav-font: 'EB Garamond', serif;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Inter', sans-serif;
            background-color: var(--bg-color);
            color: var(--text-main);
            line-height: 1.6;
            overflow-x: hidden;
        }

        /* Navigation */
        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 2rem;
        }

        header {
            padding: 3rem 0;
            display: flex;
            justify-content: space-between;
            align-items: center;
            background: #fff;
            position: sticky;
            top: 0;
            z-index: 1000;
        }

        .logo {
            font-family: var(--nav-font);
            font-size: 1.8rem;
            letter-spacing: -1px;
            font-weight: 500;
        }

        nav ul {
            display: flex;
            list-style: none;
            gap: 2.5rem;
        }

        nav ul li a {
            font-family: var(--nav-font);
            font-size: 1.1rem;
            text-transform: lowercase;
            color: var(--text-main);
            text-decoration: none;
            transition: opacity 0.3s;
        }

        nav ul li a:hover, nav ul li a.active {
            opacity: 0.5;
        }

        /* Hero Banner Section */
        .hero-banner {
            position: relative;
            width: 100%;
            height: 60vh;
            overflow: hidden;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            text-align: center;
            background: #000;
            margin-bottom: 4rem;
        }

        .hero-video {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            object-fit: cover;
            opacity: 0.5; /* Dimmed to 50% */
            z-index: 1;
        }

        .hero-content {
            position: relative;
            z-index: 2;
            color: white;
            padding: 0 1rem;
        }

        .captured-by {
            font-size: 1rem;
            vertical-align: super;
            margin-right: 5px;
            font-weight: 300;
        }

        #dynamic-title {
            font-size: 4rem;
            font-weight: 400;
            transition: font-family 0.1s ease-in-out;
            display: inline-block;
        }

        .hero-subtitle {
            margin-top: 1rem;
            font-size: 1rem;
            font-weight: 300;
            letter-spacing: 0.5px;
        }

        .hero-welcome {
            margin-top: 2rem;
            font-family: var(--nav-font);
            font-style: italic;
            font-size: 1.2rem;
            opacity: 0.9;
        }

        .adobe-text { color: var(--adobe-red); font-weight: 600; }

        /* Bio Section */
        .bio-section {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 4rem;
            align-items: center;
            padding-bottom: 6rem;
        }

        .bio-image img {
            width: 100%;
            height: auto;
            display: block;
        }

        .bio-text {
            font-size: 1.1rem;
            color: var(--text-main);
        }

        .phillis-name {
            color: var(--phillis-blue);
            font-weight: 600;
        }

        /* Portfolio Grids */
        .page { display: none; animation: fadeIn 0.8s ease; }
        .page.active { display: block; }
        @keyframes fadeIn { from { opacity: 0; } to { opacity: 1; } }

        .editorial-header {
            margin-bottom: 3rem;
            border-left: 1px solid var(--border-color);
            padding-left: 1.5rem;
        }

        .photo-grid {
            display: grid;
            grid-template-columns: repeat(2, 1fr);
            gap: 1.5rem;
            margin-bottom: 4rem;
        }

        .photo-item img { width: 100%; display: block; filter: grayscale(10%); }
        .photo-item.wide { grid-column: span 2; }

        /* Form Styling */
        .contact-form {
            max-width: 600px;
            margin: 0 auto;
            display: grid;
            gap: 1rem;
        }
        .contact-form input, .contact-form textarea {
            padding: 1rem;
            border: 1px solid var(--border-color);
            font-family: var(--nav-font);
        }
        .contact-form button {
            background: #000;
            color: #fff;
            padding: 1rem;
            border: none;
            cursor: pointer;
            font-family: var(--nav-font);
        }

        footer {
            text-align: center;
            padding: 4rem 0;
            font-size: 0.8rem;
            opacity: 0.5;
        }

        @media (max-width: 768px) {
            .bio-section { grid-template-columns: 1fr; }
            header { flex-direction: column; gap: 1.5rem; }
            #dynamic-title { font-size: 2.5rem; }
        }
    </style>
</head>
<body>

    <div class="container">
        <header>
            <div class="logo">phillyy</div>
            <nav>
                <ul>
                    <li><a href="#" class="nav-link active" data-page="hello">hello~</a></li>
                    <li><a href="#" class="nav-link" data-page="graduation">graduation</a></li>
                    <li><a href="#" class="nav-link" data-page="portraits">portraits</a></li>
                    <li><a href="#" class="nav-link" data-page="bookings">bookings</a></li>
                </ul>
            </nav>
        </header>

        <main>
            <!-- HELLO PAGE (ABOUT ME) -->
            <section id="hello" class="page active">
                <div class="hero-banner">
                    <!-- Placeholder landscape video/gif -->
                    <video class="hero-video" autoplay muted loop playsinline>
                        <source src="https://assets.mixkit.co/videos/preview/mixkit-clouds-and-blue-sky-re-flected-in-the-water-15572-large.mp4" type="video/mp4">
                    </video>
                    <div class="hero-content">
                        <div id="dynamic-container">
                            <span class="captured-by">captured by</span>
                            <span id="dynamic-title">phillyy</span>
                        </div>
                        <p class="hero-subtitle">
                            freelance photographer, berkeley student, <span class="adobe-text">Adobe</span> creative apprentice – just traversing through the universe
                        </p>
                        <div class="hero-welcome">
                            <p>welcome to my alternate universe</p>
                            <p>take your time, feel free to stay for a while~</p>
                        </div>
                    </div>
                </div>

                <div class="bio-section">
                    <div class="bio-image">
                        <img src="https://images.unsplash.com/photo-1509248961158-e54f6934749c?auto=format&fit=crop&w=1000&q=80" alt="Portrait of Phillis">
                    </div>
                    <div class="bio-text">
                        <p>hi, my name is <span class="phillis-name">phillis</span>! I’m a berkeley chemistry (sadge) student who found a creative outlet through photography. I started with creative portraitshoots and have always enjoyed the directing, planning, and executing of a creative vision with other people.</p>
                        <br>
                        <p>In this coming age of AI, let’s still find a way to connect with each other~</p>
                    </div>
                </div>
            </section>

            <!-- GRADUATION PAGE -->
            <section id="graduation" class="page">
                <div class="editorial-header">
                    <h2 style="font-family: var(--nav-font);">Class of 2024</h2>
                    <p style="color: var(--text-accent);">Capturing the transition from campus life to the real world.</p>
                </div>
                <div class="photo-grid">
                    <div class="photo-item"><img src="https://images.unsplash.com/photo-1523580494863-6f3031224c94?auto=format&fit=crop&w=800&q=80" alt="Grad"></div>
                    <div class="photo-item"><img src="https://images.unsplash.com/photo-1541339907198-e08756eaa539?auto=format&fit=crop&w=800&q=80" alt="Grad"></div>
                    <div class="photo-item wide"><img src="https://images.unsplash.com/photo-1523050853021-db9e139d82a1?auto=format&fit=crop&w=1200&q=80" alt="Grad"></div>
                </div>
            </section>

            <!-- PORTRAITS PAGE -->
            <section id="portraits" class="page">
                <div class="editorial-header">
                    <h2 style="font-family: var(--nav-font);">Identity & Light</h2>
                    <p style="color: var(--text-accent);">Explorations of the self through shadow and texture.</p>
                </div>
                <div class="photo-grid">
                    <div class="photo-item wide"><img src="https://images.unsplash.com/photo-1531123897727-8f129e1688ce?auto=format&fit=crop&w=1200&q=80" alt="Portrait"></div>
                    <div class="photo-item"><img src="https://images.unsplash.com/photo-1502033006962-3bb54bad7112?auto=format&fit=crop&w=800&q=80" alt="Portrait"></div>
                    <div class="photo-item"><img src="https://images.unsplash.com/photo-1534528741775-53994a69daeb?auto=format&fit=crop&w=800&q=80" alt="Portrait"></div>
                </div>
            </section>

            <!-- BOOKINGS PAGE -->
            <section id="bookings" class="page">
                <div style="text-align: center; margin-bottom: 3rem;">
                    <h2 style="font-family: var(--nav-font);">Reserve a session</h2>
                    <p>let's bring your creative vision to life.</p>
                </div>
                <form class="contact-form">
                    <input type="text" placeholder="Name" required>
                    <input type="email" placeholder="Email" required>
                    <textarea rows="6" placeholder="Describe your vision..." required></textarea>
                    <button type="submit">Send Request</button>
                </form>
            </section>
        </main>

        <footer>
            <p>&copy; 2024 phillyy. traversing through the universe.</p>
        </footer>
    </div>

    <script>
        // --- Navigation Logic ---
        const navLinks = document.querySelectorAll('.nav-link');
        const pages = document.querySelectorAll('.page');

        navLinks.forEach(link => {
            link.addEventListener('click', (e) => {
                e.preventDefault();
                const target = link.getAttribute('data-page');
                
                navLinks.forEach(l => l.classList.remove('active'));
                link.classList.add('active');

                pages.forEach(page => {
                    page.classList.toggle('active', page.id === target);
                });
                window.scrollTo(0,0);
            });
        });

        // --- Dynamic Font Logic ---
        const titleElement = document.getElementById('dynamic-title');
        const fonts = [
            "'EB Garamond', serif",
            "'Playfair Display', serif",
            "'Courier Prime', monospace",
            "'Inter', sans-serif",
            "'Libre Baskerville', serif"
        ];
        let fontIndex = 0;

        setInterval(() => {
            titleElement.style.fontFamily = fonts[fontIndex];
            fontIndex = (fontIndex + 1) % fonts.length;
        }, 500); // Changes every 0.5 seconds
    </script>
</body>
</html>